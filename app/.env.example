# Populated from CDK stack outputs at build time by the GitHub Actions workflow.
VITE_USER_POOL_ID=us-east-1_xxxxxxxxx
VITE_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Vite dev server proxies /api/* here (defaults to http://localhost:3000, the
# local Hono node server). Point it at the deployed HttpApi to run the frontend
# locally against real data.
# VITE_DEV_API_PROXY_TARGET=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com
